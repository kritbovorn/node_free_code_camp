const amount = 12;
const num = 14

if (amount < num) {
    console.log( amount + ' is Small Number');
} else {
    console.log( amount + ' is Larger Number');
}

console.log(` ${ amount } ### Hey it's my first node app !!!.`);